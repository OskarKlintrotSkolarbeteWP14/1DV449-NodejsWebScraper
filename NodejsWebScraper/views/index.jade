extends layout

block content
  .jumbotron
    h1 Duschskrapan
    p.lead En labb för att hitta en tid för att äta lite gott och se pilsnerfilm!
    p
    form.form-inline(method='post' action="/scrape")
        div.form-group
            label(for='urlToScrape') Ange url:
            input.form-control(type='text' name='urlToScrape' id='urlToScrape' value='http://46.101.232.43/')
            button.btn.btn-default(type='submit' placeholder='test' value='Start!') Start!
    
  p 
    - var days = ["fredag", "lördag", "söndag"]
        if message
            div
                each item in message
                    p Ni kan se 
                       strong= item.getMovie()
                       |  på 
                       strong= days[item.getDay()]
                       |  klockan
                       strong=  item.getTime()
        else
            p Skriv in en adress och tryck på "Start!" för att skrapa sidorna.